/* Diferençã entre variável e constante.
A variável pode ser manipulada e modificada, já a constante não. */

const PRECO = 2;
var desconto = 0.2;
var valor_total = PRECO - desconto;

// Funções
// Estrutura de uma função
function soma(a, b) {
    return a + b;
}
// Envocando uma função.
soma(3, 5);
